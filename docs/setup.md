# Setup

1. Clone: `git clone https://github.com/Ananttyagi07/KubeSimplify.git && cd KubeSimplify`
2. Venv: `python3 -m venv venv && source venv/bin/activate`
3. Deps: `pip install -r requirements.txt`
4. Kubeconfig: Ensure `~/.kube/config` or in-cluster
5. Run: `python main.py --help`

